import { Reserve, Product } from '../shared/models/reserve';

export class Calculation {

    private reserve: Reserve;

    constructor(_reserve: Reserve) {
        this.reserve = _reserve;
    }

    public Calculate() {

    }
}
